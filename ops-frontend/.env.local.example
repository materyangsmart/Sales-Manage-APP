# ============================================================
# ops-frontend 本地开发环境变量配置示例
# ============================================================
# 使用方法：
#   1. 复制此文件，重命名为 .env（不带 .example 后缀）
#   2. 填写下方的配置值
#   3. 重启前端服务
#
# 注意：.env 文件已被 .gitignore 忽略，不会被提交到 GitHub
# ============================================================

# ============================================================
# 数据库配置（Manus 云平台自动注入，本地需手动配置）
# ============================================================
# 如果你使用 Manus 云平台，此变量会自动注入，无需手动配置
# 如果你在本地运行，填写你的 MySQL 连接字符串
DATABASE_URL=mysql://root:你的密码@127.0.0.1:3306/qianzhang_sales

# ============================================================
# OAuth 认证配置（Manus 云平台自动注入，本地需手动配置）
# ============================================================
# 警告：如果此变量未配置，前端会崩溃并显示 "TypeError: Invalid URL"
# 本地开发时，可以填写 Manus 的 OAuth 门户 URL
# 格式：https://xxx.manus.im 或 https://portal.manus.im
VITE_OAUTH_PORTAL_URL=https://portal.manus.im

# Manus OAuth 应用 ID（从 Manus 控制台获取）
VITE_APP_ID=你的AppID

# OAuth 服务器 URL（后端使用）
OAUTH_SERVER_URL=https://api.manus.im

# ============================================================
# 后端 API 配置
# ============================================================
# 后端 NestJS 服务地址（默认端口 3100）
BACKEND_URL=http://localhost:3100
VITE_BACKEND_URL=http://localhost:3100

# ============================================================
# 内部服务 Token（Manus 云平台自动注入，本地需手动配置）
# ============================================================
# 用于前端与后端之间的内部通信认证
# 本地开发时可以填写任意字符串（如 local-dev-token）
INTERNAL_SERVICE_TOKEN=local-dev-token
VITE_INTERNAL_TOKEN=local-dev-token

# ============================================================
# JWT 配置
# ============================================================
# 用于 Session Cookie 签名，本地开发时可以填写任意字符串
JWT_SECRET=local-jwt-secret-change-in-production

# ============================================================
# 应用信息（可选）
# ============================================================
VITE_APP_TITLE=千张销售管理系统 - 内部中台工作台
