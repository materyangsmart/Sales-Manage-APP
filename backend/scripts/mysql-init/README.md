# MySQL初始化脚本

此目录中的SQL脚本会在MySQL容器首次启动时自动执行。

## 说明

- 脚本按文件名字母顺序执行
- 建议使用数字前缀命名，如 `01-init.sql`, `02-users.sql`
- 脚本只在容器首次创建时执行，如需重新执行，需要删除volume

## 注意事项

- 不要在此目录放置敏感数据
- 生产环境的初始化应该使用专门的迁移工具
- 此目录主要用于开发和测试环境的快速初始化
